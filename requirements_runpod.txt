# RunPod requirements — install with:
#   pip install -r requirements_runpod.txt
#
# PyTorch is pre-installed on RunPod GPU images. If it is missing or you need
# a specific CUDA version, run first:
#   pip install torch>=2.0 --index-url https://download.pytorch.org/whl/cu121

numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
